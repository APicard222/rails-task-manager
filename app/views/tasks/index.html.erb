<h1>My Tasks</h1>
<ul>
<% @tasks.each do |task|%>
<li><%= link_to task.title, task_path(task)%> - <%= task.details %> - <%= link_to "🗑️",
  task_path(task),
  method: :delete,
  data: { confirm: "Are you sure?" } %></li>
<% end%>
</ul>

+ <%=  link_to "Create a new Task", tasks_new_path%>
